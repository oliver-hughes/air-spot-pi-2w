# GENERATED by air-spot-pi-2w bootstrap. Local edits will be overwritten.

camilla_host: "127.0.0.1"
camilla_port: 1234

# Exposed on the LAN with no authentication -- this is a home network and
# needing an SSH tunnel to nudge an EQ band is friction you'd stop tolerating
# within a week. Change to 127.0.0.1 and tunnel if you'd rather lock it down.
bind_address: "@GUI_BIND@"
port: @GUI_PORT@

config_dir: "/etc/camilladsp/configs"
coeff_dir: "/etc/camilladsp/coeffs"
default_config: "/etc/camilladsp/configs/active.yml"
active_config: "/etc/camilladsp/configs/active.yml"

# Let the GUI write the active config directly, so changes take effect without
# a round trip through the repo. `make pull-config` is how they get back into
# git -- see docs/TUNING.md.
update_config_symlink: false
update_config_txt: false
on_set_active_config: none
on_get_active_config: none

supported_capture_types: ["Alsa"]
supported_playback_types: ["Alsa"]
